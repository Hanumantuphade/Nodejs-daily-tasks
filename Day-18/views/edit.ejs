<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="w-full min-h-screen bg-zinc-900 p-10">
      <h3 class="text-sm text-white mt-2">you can edit your post ...</h3>
      <!-- Create Post Form -->
      <form action="/update/<%= post._id%>" method="post" class="mt-3 text-white">
        <textarea
          class="w-1/3 resize-none mb-2 block text-sm rounded-md outline-none border-2 border-zinc-800 bg-transparent p-2"
          placeholder="what's in your mind ?"
          name="content"
        ><%= post.content%></textarea>
        <input
          class="block bg-yellow-500 text-black rounded-md px-3 text-sm py-2 mb-2 cursor-pointer"
          type="submit"
          value="Update Post"
        />
      </form>
    </div>
  </body>
</html>
