<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="main w-full min-h-screen bg-zinc-900">
      <div class="form p-10 text-white">
        <form action="/create " method="POST">
          <input
            name="title"
            class="block w-full rounded-md outline-none bg-zinc-700 px-5 py-2"
            type="text"
            placeholder="title goes here"
          />
          <textarea
            name="details"
            class="block w-full rounded-md outline-none bg-zinc-700 mt-5 px-2 py-2 resize-none"
            placeholder="Write your task details here.."
          ></textarea>
          <input
            type="submit"
            value="Create task"
            class="bg-blue-500 mt-5 text-white px-4 py-2 rounded"
          />
        </form>
      </div>
      <div class="tasks p-10 flex flex-wrap gap-5">
        <% if(files.length > 0){%> <% files.forEach((val)=>{%>
        <div class="task w-72 px-4 py-3 rounded-md bg-zinc-700">
          <h1 class="text-white text-2xl tracking-tighter"><%= val%></h1>
          <a class="text-blue-500 mt-3 inline-block">READ MORE</a>
        </div>

        <%})%> <% } else {%>
        <h3 class="text-zinc-600">No TASKS YAT</h3>

        <% } %>

        <!-- <div class="task hidden w-72 px-4 py-3 rounded-md bg-zinc-700">
          <h1 class="text-white text-2xl tracking-tighter">something</h1>
          <a class="text-blue-500 mt-3 inline-block">READ MORE</a>
        </div>
        <div class="task hidden w-72 px-4 py-3 rounded-md bg-zinc-700">
          <h1 class="text-white text-2xl tracking-tighter">something</h1>
          <a class="text-blue-500 mt-3 inline-block">READ MORE</a>
        </div> -->
      </div>
    </div>
  </body>
</html>
